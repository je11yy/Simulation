Приложение, моделирующее работу центра клиентской поддержки.

UPD к основному тз: помимо количества операторов для каждого отделения так же должен быть указан идентификатор (целое неотрицательное число).

Моделирование: от времени начала до времени конца с промежутком в минуту
1. Извлекается заявка, время поступления которой равно текущему времени, не учитывая секунды (для этого массив заявок должен быть заранее отсортирован по времени поступления)
2. Заявка добавляется в соответствующее отделение
3. Проверка на загруженность отделений
4. Разгрузка операторов, закончивших работу
4. Загрузка операторов заявками

applications_generator.c - генерирует файл с заявками. 
Если нет аргументов, название файла на выходе "applications.txt", иначе название - первый аргумент командной строки

info_file_generator.c - с помощью интерактивного диалога с пользователем генерирует файл с настройками. Файл на выходе - "info.txt"

Папка storages:
    /applications - все структуры для хранения заявок
    /departments - все структуры для хранения отделений
    application_storage - интерфейс для структур, хранящих заявки
    department_stprage - интерфейс для структур, хранящих отделения

Результат моделирования находится в файле "simulation_logging.txt"

Проверка на утечки: выполнена, утечек при использовании любой из структур не выявлено